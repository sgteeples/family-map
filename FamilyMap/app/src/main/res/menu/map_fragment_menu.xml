<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search_menu_item"
        android:icon="@drawable/mag_glass_icon"
        android:title="@string/searchMenuOptionTitle"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/filter_menu_item"
        android:icon="@drawable/filter_icon"
        android:title="@string/filterMenuOptionTitle"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/settings_menu_item"
        android:icon="@drawable/gear_icon"
        android:title="@string/settingsMenuOptionTitle"
        app:showAsAction="ifRoom|withText" />
</menu>